apiVersion: 1

providers:
  - name: 'default'
    orgId: 1
    folder: '0xCultiv8'
    folderUid: 'cultiv8'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
